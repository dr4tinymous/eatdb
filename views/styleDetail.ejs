<!DOCTYPE html>
<html lang="en">
	<%- include('partials/head') %>
<body>
    <h1><%= style.name %></h1>
    <h2>Recipes with this style:</h2>
    <ul class="list-container">
        <% style.recipes && style.recipes.forEach(recipe => { %>
            <li><a href="/recipes/<%= recipe.id %>"><%= recipe.title %></a></li>
        <% }); %>
    </ul>
    <%- include('partials/footer.ejs') %>
</body>
</html>
